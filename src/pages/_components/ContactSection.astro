<!-- Contact section -->
<div id="contact" class="p-div c_container">
  <section class="contact-section">
    <div class="c-contact-title">
      <h2 class="contact-title">Contacto</h2>
    </div>
    <div class="contact-form-container">
      <div class="c-contact-text">
        <!-- <p class="contact-text">
          Estoy disponible tanto para trabajos freelance como para nuevas
          oportunidades laborales. Si tienes un proyecto en mente, una pregunta,
          o simplemente quieres saludar, no dudes en contactarme.
        </p> -->
      </div>
      <div class="contact-form">
        <form action="https://formspree.io/f/xzbqnldp" method="POST">
          <div class="c-inputs">
            <div class="form-control input-half">
              <label for="name">Nombre</label>
              <input
                type="text"
                id="name"
                name="sender-name"
                placeholder="Nombre"
                class="input-field nes-input"
                required
              />
            </div>
            <div class="form-control input-half">
              <label for="email">Email</label>
              <input
                type="email"
                id="email"
                name="sender-email"
                placeholder="Email o red social"
                class="input-field nes-input"
                required
              />
            </div>
          </div>
          <div class="form-control">
            <label for="message">Mensaje</label>
            <textarea
              id="message"
              cols="3"
              rows="4"
              placeholder="Mensaje"
              name="message"
              class="input-field nes-textarea"
              required></textarea>
          </div>
          <div class="c-submit-btn">
            <button
              type="submit"
              value="Enviar"
              id="submit-btn"
              class="nes-btn is-error submit-btn"
            >
              Enviar
            </button>
          </div>
        </form>
      </div>
    </div>
  </section>
</div>
