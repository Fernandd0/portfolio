<!-- Contact section -->
<div id="contact" class="hidden-div"></div>
<section class="contact-section c_container">
  <div class="c-contact-title">
    <h2 class="contact-title">Ponerse en contacto</h2>
  </div>
  <div class="contact-form-container">
    <div class="c-contact-text">
      <p class="contact-text">
        Estoy disponible tanto para trabajos freelance como para nuevas
        oportunidades laborales. Si tienes un proyecto en mente, una pregunta, o
        simplemente quieres saludar, no dudes en contactarme.
      </p>
    </div>
    <div class="contact-form">
      <form action="https://formspree.io/f/xzbqnldp" method="POST">
        <div class="form-control">
          <label for="name">Nombre</label>
          <input
            type="text"
            id="name"
            name="sender-name"
            placeholder="Nombre"
            class="input-field nes-input"
            required
          />
        </div>
        <div class="form-control">
          <label for="email">Email</label>
          <input
            type="email"
            id="email"
            name="sender-email"
            placeholder="Email, Twitter o Linkedin"
            class="input-field nes-input"
            required
          />
        </div>
        <div class="form-control">
          <label for="message">Mensaje</label>
          <textarea
            id="message"
            cols="3"
            rows="3"
            placeholder="Mensaje"
            name="message"
            class="input-field nes-textarea"
            required></textarea>
        </div>
        <div class="c-submit-btn">
          <input
            type="submit"
            value="Enviar"
            id="submit-btn"
            class="nes-btn submit-btn"
          />
        </div>
      </form>
    </div>
  </div>
</section>
